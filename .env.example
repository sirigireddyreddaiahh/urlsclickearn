# Copy this file to .env and fill the values for local development / deployment
# Do NOT commit real secrets to the repository.

# App secrets
NUXT_JWT_SECRET=changeme_super_secret_key

# OAuth (optional for local dev unless you test OAuth flows)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
OAUTH_BASE_URL=http://localhost:3000

# Cloudflare & deployment (only required for deployment or Cloudflare API usage)
BREVO_API_KEY=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# Public runtime flags
NUXT_PUBLIC_PREVIEW_MODE=false
NUXT_PUBLIC_SLUG_DEFAULT_LENGTH=6
NUXT_SITE_TOKEN=Urlsclickearn
NUXT_REDIRECT_STATUS_CODE=308
NUXT_LINK_CACHE_TTL=3600
NUXT_REDIRECT_WITH_QUERY=false
NUXT_HOME_URL=http://localhost:3000

# Notes:
# - For local development you only strictly need NUXT_JWT_SECRET and optionally NUXT_HOME_URL.
# - For OAuth flows, fill the provider client IDs and secrets.
# - For Cloudflare/Wrangler preview and deploy, set BREVO_API_KEY and CLOUDFLARE_* values.
